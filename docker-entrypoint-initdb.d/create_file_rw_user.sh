#!/bin/bash
# Grant read/write permissions to the var/uploads folder
#useradd -m -s /bin/bash $FILE_USER
#passwd $FILE_USER
#chown $FILE_USER:$FILE_USER $FILE_UPLOADS_PATH
#chmod 770 $FILE_UPLOADS_PATH
